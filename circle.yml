machine:
  ruby:
    version: 2.3.1
  environment:
    KITCHEN_YAML: ~/chef-opsview-client/.kitchen.circle.yml
    machine_user: circle
    machine_pass: circle
    machine_port: 22

dependencies:
  pre:
    - sudo usermod -p "`openssl passwd -1 'circle'`" circle

test:
  post:
    - bundle exec rake style
    - bundle exec kitchen test client-ubuntu-1404
